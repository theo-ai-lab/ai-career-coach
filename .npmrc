# Required for @langchain/* + Next 16 + React 19 peer-dep mismatch.
# Without this, `npm install` errors on the langchain ecosystem's peer
# dependency declarations against older React majors. Safe to remove
# once the LangChain packages publish React-19 peer ranges.
legacy-peer-deps=true
